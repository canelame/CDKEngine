#include "geometry.h"

void Geometry::loadObjFile(const char *file){
  /*std::string name = "cube.obj";
  std::string err = tinyobj::LoadObj(shapes_, materials_, name.c_str());
  if (!err.empty()){
    
  }*/
}